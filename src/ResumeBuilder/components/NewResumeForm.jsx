import React, { useState } from 'react'
import {
    Box,
    Stack,
    FormControl,
    FormLabel,
    Input,
    Button,
    useToast,
    Heading,
} from '@chakra-ui/react'

const NewResumeForm = ({ resume, setResume }) => {
    return (
        <Stack
            direction={'row'}
            alignItems={'flex-start'}
            justifyContent={'flex-start'}
            w="100%"
            css={{ backgroundColor: 'gray.100' }}
            spacing={4}
            p={4}
            borderRadius="lg"
            boxShadow="lg"
            roundedTop={'none'}
            bg={'blue'}
        >
            
        </Stack>
    )
}


export default NewResumeForm